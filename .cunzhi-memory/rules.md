# 开发规范和规则

- 此项目每次修改/变更代码时，必须强制增加版本号 (@version in user.js and version in manifest.json)，否则用户端可能无法自动拉取更新。
- 用户新增强制约束：不要生成总结性Markdown文档；不要生成测试脚本；不要编译；不要运行，均由用户自行处理。
- 用户新增强制约束：不要生成总结性Markdown文档；不要生成测试脚本；不要编译；不要运行，均由用户自行处理。
